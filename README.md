# tcc-tads

```bash
# Listar devices conectados
adb devices

# Rodar projeto no device (ver o código com adb devices)
npx react-native run-android --device <deviceId>

# Rodar o metro
npx react-native start
```