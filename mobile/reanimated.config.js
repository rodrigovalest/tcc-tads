module.exports = {
  strict: false,
  verbose: false,
  // Fix for backmaping error
  disableInlineStylesWarning: true,
  // Disable problematic features that cause reflection errors
  disableWorkletCache: true,
  // Additional fixes for reflection issues
  disableWorkletCache: true,
  disableInlineStylesWarning: true,
  // Disable problematic reflection features
  disableReflectionWarnings: true,
};
